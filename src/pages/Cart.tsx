import React from 'react'
import CartContainer from '../features/cart/CartContainer'

const Cart: React.FC = ({}) => {
  return (
    <div>
      <CartContainer />
    </div>
  )
}

export default Cart
