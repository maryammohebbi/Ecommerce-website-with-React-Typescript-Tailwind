import React from 'react'
import AuthContainer from '../authentication/AuthContainer'

const Auth: React.FC = ({}) => {
  return (
    <div>
      <AuthContainer />
    </div>
  )
}

export default Auth
